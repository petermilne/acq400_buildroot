#!/bin/sh

if [ "x$*" = "x" ]; then
	IPX=$(/sbin/ifconfig | grep inet[^6] | awk '{ print $2 }')
	for IP in $IPX; do
		echo ${IP#*:*}
	done
else
	for ethx in $*; do
		IPX=$(/sbin/ifconfig $ethx | grep inet[^6] | awk '{ print $2 }')
		echo ${IPX#*:*}
	done
fi

